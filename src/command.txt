### compile the src
make
### configure the .gdbinit file and modify the path to your own!!!
echo "add-auto-load-safe-path .gdbinit" > /home/zhou/.gdbinit
### normal mode
../bochs-2.6.9/bochs -q -f bochsrc-hd.bxrc
### debug mode, here the terminal is lxterminal, if you know how to launch normal terminal on ubuntu, contact with me!!!
lxterminal -T "gdb --quiet kernel.sym" -e "gdb --quiet kernel.sym" & ../bochs-2.6.9/bochs -q -f bochsrc-hd.bxrc.dbg